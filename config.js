(() => {
  // Set this once to your deployed backend URL (no trailing slash).
  // Example: https://investolab-api.onrender.com
  window.INVESTOLAB_API_BASE = window.INVESTOLAB_API_BASE || '';
})();
